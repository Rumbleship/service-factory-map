{
  /* THis is used for the vscode and test */
  "extends": "./tsconfig-base.json",
  "compilerOptions": {
    "outDir": "./test_out" /* Redirect output structure to the directory. */,
  },
  "include": ["./src/**/*.ts", "./test/**/*.ts"],
}
